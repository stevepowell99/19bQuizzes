---
title: Transforms filters
---

> Check out these links from the Garden: [Extensions overview](https://garden.causalmap.app/extensions/) | [Path tracing filter](https://garden.causalmap.app/path-tracing-filter/)

What is the main purpose of transforms filters in Causal Map?
y To enhance, simplify or modify the structure of the map for analysis.
To translate the data into different languages automatically using the built-in translation engine.
To compress the data for faster processing.
To encrypt the data for online sharing.
y To let users focus on specific aspects, such as combining similar factors or simplifying complex maps.
To increase the resolution of causal maps so they print clearly on A3 paper.

What is an example of how a transform filter might be used in Causal Map?
y To combine factors that are conceptually similar to reduce complexity.
To filter out statements made before a certain date using a built-in date range selector.
To automatically categorise all statements by their sentiment score.

What might be a reason to use a transform filter to `focus` on specific factors?
y To see the factors which influence, or are influenced by, one or more factors of interest.
To hide sensitive factors and anonymise respondent data for data privacy compliance requirements.
To trace all paths of influence of one factor on another.

What might be a reason to use a `trace paths` filter?
To see the factors which influence, or are influenced by, one or more factors of interest.
To hide sensitive factors for data privacy compliance.
y To trace all the paths of influence of one factor on another.

How do transform filters facilitate the comparison of different subgroups in Causal Map?
You use the `surprise` filter in Map Formatting.
By merging datasets from different sources into a single unified file so all subgroups appear together.
By highlighting differences in data coding practices across subgroups using colour overlays.
y By using Source Groups filters in the pipeline to select each subgroup separately.

How do you access the Transforms Filters in the Causal Map interface?
y It is a sub-panel on the left hand side of the app.
By right-clicking on any factor or link within the causal map.
Through a dedicated button on the main toolbar.
Via the settings menu.

How can users apply a filter to focus on specific factors?
y By using the "Focus on Factors" filter and specifying which factors to include or exclude.
By selecting factors on the map, which automatically applies a focus filter and hides all others.
Through a voice command feature integrated with the browser's speech recognition API.
By importing a predefined list of factor names from an external CSV or JSON file.

In the Transforms Filters interface, how do users exclude specific factors from their analysis?
y With the "Exclude Factors" option, removing selected factors from the current view.
By dragging unwanted factors into a trash bin icon on the right-hand panel.
By toggling a visibility switch on each factor individually to mark it as "hidden."
Using a colour-coding system to mark excluded factors so they appear greyed out but remain visible.

What feature in the Transforms Filters interface helps users simplify complex causal maps?
y The "Collapse Factors" filter, which consolidates multiple factors into broader categories.
A "Simplify Map" button that uses AI to automatically reduce the number of links and factors shown.
A manual drawing tool that lets users redraw and reorganise the map layout from scratch.
A complexity slider that dynamically adjusts the level of detail displayed in the map.

Which Transforms Filter should you use for analysing paths between factors?
y The "Tracing Paths" filter, which highlights pathways between selected factors.
A dedicated path analysis mode accessible from the main menu under Analysis.
A special link drawing tool that lets users manually trace and annotate individual paths.
A search box that automatically traces all shortest paths between any two named factors.

What interface option allows users to revert the changes made by Transforms Filters?
y The "Reset Filters" button, which clears all applied transform filters.
An undo feature targeting the last applied transform filter.
A history panel for reverting specific filter applications.
A "Revert All" option in the file menu.

How can users save the configuration of their applied Transforms Filters for future use?
y By using Bookmarks, allowing them to quickly reapply the same filter state in future sessions.
By bookmarking the current page URL in their web browser, which stores the full filter state in the address bar.
By taking a screenshot of the filter settings and reapplying them manually.
By exporting the filter configuration to an external file.
